import React from "react";

const ProjectList = () => {
  return (
    <div>
      <h1>ProjectList</h1>
      {/* <SearchBar /> */}
      {/* projects.map(e => <ProjectListItem key={e.uid}>{e.projectName}</ProjectListItem>) */}
    </div>
  );
};

export default ProjectList;
