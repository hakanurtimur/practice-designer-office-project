import React from "react";

const MessageList = () => {
  return (
    <div>
      <h1>MessageList</h1>
      {/* users.map(e => <UserListItem key={e.uid}>{e.userName}</UserListItem>) */}
    </div>
  );
};

export default MessageList;
