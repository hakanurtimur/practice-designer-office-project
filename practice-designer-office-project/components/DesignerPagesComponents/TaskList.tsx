import React from "react";

const TaskList = () => {
  return (
    <div>
      <h1>Task List</h1>
      {/* tasks.map(task => <TaskListItem task={task} />) */}
    </div>
  );
};

export default TaskList;
