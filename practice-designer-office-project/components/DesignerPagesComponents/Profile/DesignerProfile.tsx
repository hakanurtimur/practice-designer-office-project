import React from "react";
import DefaultProfile from "@/components/helpers/DefaultProfile/DefaultProfile";

const DesignerProfile = () => {
  return <DefaultProfile userRole={"designer"} />;
};

export default DesignerProfile;
